# libcamera's software ISP needs the dma-buf heap. Numbered 60 so the tag is
# set before 70-uaccess.rules, which is what applies the session ACL.
SUBSYSTEM=="dma_heap", KERNEL=="system", TAG+="uaccess"
